{
    "scripts": {
        "build": "esbuild src/main.tsx --bundle --outfile=public/main.js --sourcemap",
        "watch": "esbuild src/main.tsx --bundle --outfile=public/main.js --sourcemap  --watch",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "esbuild": "^0.14.1"
    },
    "devDependencies": {
        "@types/react-dom": "^17.0.11",
        "react": "^16.13.1",
        "react-dom": "^16.13.1"
    }
}